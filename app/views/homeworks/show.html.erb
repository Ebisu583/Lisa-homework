<div class="container mt-5">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1><%= @homework.title %></h1>
    <%= link_to edit_homework_path(@homework.id), class: 'btn btn-light btn-sm border' do %>
      <i class="fa fa-pencil"></i> Edit this homework
    <% end %>
  </div>
</div>
